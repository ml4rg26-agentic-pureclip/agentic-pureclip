\section{Architecture \& Implementation}
\label{method}

\scaffold{Bullets to expand. Architecture/methods, what was built and why.}
\begin{itemize}
  \item \textbf{Datasets \& registry.} 5 original + an ENCORE RBFOX2 dataset + 20 new (2026-07 ENCODE/Drive batches), \textbf{26 datasets across 15 RBPs} (12 newly added), in a table-driven registry (\texttt{pipeline/datasets.py}, \texttt{\_standard\_spec}); motifs auto-resolve per RBP/cell-line from a PWM catalog. All GRCh38, deduplicated; chr21 ``fast mode'' throughout ($\sim$minutes/trial vs.\ hours genome-wide).
  \item \textbf{Shared evaluation.} One \texttt{evaluate\_config()} entry point (Snakemake: merge IP $\rightarrow$ \texttt{pureclip2} $\rightarrow$ per-replicate $\rightarrow$ post-process $\rightarrow$ score $\rightarrow$ \texttt{score\_report.json}), called identically by both optimizers so the comparison isolates \emph{proposal strategy}, not pipeline differences.
  \item \textbf{Tunable parameters (7).} PureCLIP: \texttt{bandwidth\_nt}[20--100], \texttt{merge\_distance\_nt}[4--16], \texttt{high\_precision\_mode}, \texttt{use\_input\_covariate}; post-processing: \texttt{min\_crosslink\_events}[2--6], \texttt{force\_width}[3--15], \texttt{cluster\_gap\_width}[4--16].
  \item \textbf{Composite objective (shared evaluation, not strategy).} $(0.5\cdot\text{repro} + 0.25\cdot\text{motif} + 0.25\cdot\text{recall}) \times \min(1, n_{\text{sites}}/10)$. The collapse guard was added after Optuna collapsed QKI to 1 ``perfect'' site for a fake composite of 0.78. \emph{Both} optimizers are scored by this identical objective, that is what makes the comparison apples-to-apples, but they use it differently: Optuna optimises the composite \emph{scalar} directly via TPE, whereas the LLM additionally sees the three \emph{decomposed} terms plus biological priors and per-iteration deltas and proposes one reasoned change. So the formula is the shared yardstick, and only the proposal strategy differs.
  \item \textbf{Motif-aware scoring.} Log-odds PWM hit-rate vs.\ shuffled background, reported for the most-enriched motif. Implementation note worth a paragraph: RBPmap \texttt{*\_PSSM} matrices carry \emph{negative} cells, which broke the log-odds conversion (\texttt{math domain error}), fixed by clamping cells before smoothing; this is what crashed HNRNPK scoring.
  \item \textbf{LLM optimizer.} DeepSeek \parencite{deepseek2024} (\texttt{deepseek-chat}, low temperature); each iteration receives biological priors (protein, motif, $\sim$9-nt footprint), bounds, current params, and a per-iteration score-delta table; proposes one change with reasoning; JSON validated against bounds with up to 3 retries.
  \item \textbf{Optuna optimizer.} TPESampler (seed 42), same \texttt{evaluate\_config}, larger trial budget for fairness.
  \item \textbf{Reproducible infrastructure (engineering contribution).} Manifest-driven batch runners (\texttt{batch\_runner.py}, \texttt{overnight\_batch.py}) and a live dashboard (Python JSON API + React SPA) that imports the \emph{same} \texttt{composite\_objective} so the UI never disagrees with the optimizer; per-dataset explorer, binding-site ``docking'' view, PDF export.
  \item \textbf{Reproducibility \& compute.} All runs are driven by versioned Snakemake configs; every trial appends to a per-run \texttt{decisions.jsonl} (params, scores, reasoning), so the whole search is auditable. Code + configs are released, and the full per-dataset ENCODE accession list is given in the Appendix.
  \item \textbf{Known method limitation.} No positional-motif prior is implemented: for the position-defined binders (U2AF1/2, SF3B1/4, RBM22) the auto-resolved target motif is only a weak proxy, so their motif term should be read with care (discussed in \S\ref{discussion}).
  % Numbers to fill (not printed): total evaluations + wall-clock under chr21
  %   fast mode, once the comparison batch finishes.
  % Figure to add (not printed): pipeline flowchart; cite tool versions
  %   (PureCLIP 2.0.4 fork, Snakemake, samtools).
\end{itemize}

% Goal: describe what was built and why each design decision was made.
% This is an architecture/methods section, not a biology section.

% ── 1. Datasets ────────────────────────────────────────────────────────
%   TABLE, 5 datasets:
%   Dataset       | RBP    | Cell  | ENCODE accessions (IP rep1, IP rep2, SMInput)
%   RBFOX2_K562   | RBFOX2 | K562  | ENCFF537RYR, ENCFF296GDR, ENCFF212IIR
%   RBFOX2_HepG2  | RBFOX2 | HepG2 | ENCFF239CML, ENCFF170YQV, ENCFF515BTB
%   QKI_K562      | QKI    | K562  | ENCFF698BKX, ENCFF012WMS, ENCFF070RME
%   QKI_HepG2     | QKI    | HepG2 | ENCFF567ADV, ENCFF862YVK, ENCFF015GLL
%   PUM1_K562     | PUM1   | K562  | ENCFF064COB, ENCFF583QFB, ENCFF222HEX
%   All BAMs: GRCh38, deduplicated (ENCODE pipeline). chr21 fast mode
%   used throughout (~minutes/trial vs. hours genome-wide).

% ── 2. Evaluation pipeline ─────────────────────────────────────────────
%   Single shared evaluate_config() entry point called by both optimizers:
%   (1) Merge IP replicates (samtools).
%   (2) Run pureclip2 on merged BAM → binding_regions.bed.
%   (3) Run pureclip2 per-replicate → per-replicate regions.
%   (4) Post-process: filter by min_crosslink_events, force footprint
%       width → binding_sites.bed.
%   (5) Score → score_report.json.
%   Figure: pipeline flowchart.

% ── 3. Tunable parameters ──────────────────────────────────────────────
%   TABLE, 7 parameters with bounds and what they control:
%   PureCLIP:        bandwidth_nt [20–100], merge_distance_nt [4–16],
%                    high_precision_mode {0,1}, use_input_covariate {0,1}
%   Post-processing: min_crosslink_events [2–6], force_width [3–15],
%                    cluster_gap_width [4–16]
%   Justify why each is tunable vs. fixed (brief sentence per group).

% ── 4. Composite objective function ───────────────────────────────────
%   composite = (0.5·repro + 0.25·motif + 0.25·recall)
%               × min(1, n_sites / 10)
%   Justify weights: reproducibility = strongest evidence of genuine
%   binding; motif + recall = biological validity + known-site recovery.
%   Collapse guard: prevents degenerate solutions (1–2 "perfect" sites);
%   motivated by an observed Optuna failure (QKI collapsed to 1 site,
%   composite 0.78) before the guard was introduced.

% ── 5. LLM optimizer ──────────────────────────────────────────────────
%   Model: DeepSeek (deepseek-chat, T=0.2). Each iteration receives:
%   biological priors (protein, motif, footprint ~9 nt), search bounds,
%   current parameters, and a progress table with per-iteration score
%   deltas. Proposes one parameter change with reasoning.
%   Validation: JSON parsed, checked against bounds + prior trials;
%   up to 3 retries with rejection reason in prompt.
%   Stopping: max_iterations (10–12) or 10 steps without improvement.

% ── 6. Optuna optimizer ───────────────────────────────────────────────
%   TPESampler (seed 42), maximising composite_objective.
%   Budget: 12–15 trials. No domain knowledge; purely data-driven.
%   Same evaluate_config() call, same decisions.jsonl output format.
%   KEY DESIGN DECISION: identical evaluation for both optimizers ensures
%   the comparison isolates proposal strategy, not pipeline differences.
