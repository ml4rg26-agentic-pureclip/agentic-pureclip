\section{Results}
\label{results}

\scaffold{Bullets to expand. Report numbers from \texttt{results/} (dashboard \texttt{/api/status}); mark the budget asymmetry as a caveat.}
\begin{itemize}
  \item \textbf{Baseline vs.\ LLM vs.\ Optuna head-to-head (original 5 datasets).} Report the \texttt{bigrun2} comparison (both optimizers, collapse-guarded objective), per-dataset winner + overall, \emph{against a default-parameter baseline}: does either optimizer actually beat untuned PureCLIP? (Without this reference point the head-to-head is uninterpretable.)
  \item \textbf{New splicing factors, preliminary chr21 (LLM).} Composites observed so far, e.g.\ U2AF2\_K562 $\approx$0.44 (highest of the new set), PTBP1\_HepG2 $\approx$0.28, HNRNPK\_K562 $\approx$0.23 / HepG2 $\approx$0.21 (after the PSSM fix). 18/20 completed on the first pass; HNRNPK required the log-odds fix.
  \item \textbf{Biological quality.} Reproducibility (RBFOX2\_K562 hardest, $\approx$0.10); motif-enrichment table (best motif, hit-rate, background, enrichment, canonical Yes/No, PWM source); benchmark recall on chr21.
  \item \textbf{Cross-cell-line.} Same RBP in K562 vs.\ HepG2, do optimal parameters differ (validates the dataset-dependent tuning premise)?
  \item \textbf{Collapse-guard effectiveness.} $n_{\text{sites}}$ distribution across trials; before/after for Optuna if pre-guard data exists.
  \item \textbf{Caveats on the numbers.} All results are chr21 (fast mode) only, with no full-genome confirmation yet, and each config is run once (no repeated seeds), so we report point estimates without a significance or variability test.
  % Status note (not printed): the head-to-head table for the 20 new datasets is
  %   pending, the comparison batch (config/insight_llm_vs_optuna.yaml) is running.
  % Figure to add (not printed): per-dataset convergence plots (composite vs.
  %   iteration, LLM vs. Optuna, baseline marked), exportable from the dashboard.
\end{itemize}

\alertbox{\textbf{Motif-difficulty side-experiment.} A focused run
(\texttt{config/insight\_llm\_vs\_optuna.yaml}: PUM2 / SRSF1 / HNRNPK / U2AF2 /
SF3B4, K562, both optimizers) tests whether the LLM's margin over Optuna widens
from crisp $\rightarrow$ degenerate $\rightarrow$ positional motifs. This batch
is still running; if the numbers arrive in time we report them here, otherwise
we carry the hypothesis into the Discussion only.}

% ── 1. LLM vs. Optuna head-to-head ────────────────────────────────────
%   TABLE, one row per dataset × optimizer:
%   Dataset | Optimizer | Best composite | Repro | Motif | Recall | n_sites
%   State which optimizer won per dataset and overall. Note the budget
%   asymmetry (LLM 10–12 iters, Optuna 12–15) as a caveat.
%   Multi-panel figure: iteration vs. composite for both optimizers,
%   default-parameter baseline marked. Highlights convergence speed
%   and whether LLM reasoning led to stepwise improvements.

% ── 2. Biological quality of the optimized sites ──────────────────────
%   2a. Reproducibility, bar chart per dataset: observed vs. chance-
%       expected overlap and the corrected score. Highlight RBFOX2_K562
%       as the hardest case (baseline ≈ 0.10).
%   2b. Motif enrichment, table: best motif, hit rate, background rate,
%       enrichment, canonical match (Yes/No), PWM source. Note any
%       dataset where the canonical motif is not top-ranked.
%   2c. Benchmark recall, per-dataset recovery of ENCODE reference
%       regions (chr21). Note recall vs. n_sites trade-off if visible.

% ── 3. Dataset-specific findings ──────────────────────────────────────
%   • Which parameters converged to similar values across datasets
%     (suggesting universal optima) vs. diverged (dataset-dependent)?
%   • RBFOX2_K562 vs. RBFOX2_HepG2: do optimal parameters differ for
%     the same RBP in different cell lines? (validates tuning premise)
%   • Any dataset where the LLM's reasoning trace reveals a biologically
%     interpretable decision (e.g. "widening bandwidth improved repro").

% ── 4. Collapse guard effectiveness ───────────────────────────────────
%   n_binding_sites distribution across all trials. Demonstrate healthy
%   yields under the guard; if pre-guard data exists, before/after
%   comparison for Optuna.
