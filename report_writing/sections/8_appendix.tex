\section{Supplementary Material}
\label{supplementary}

% This file was \include{}d by YOUR_thesis.tex but did not exist, which broke
% the build. Stubbed so the document compiles; fill or fold into §7.

\scaffold{Bullets to expand — supplementary tables/figures that support the main text.}
\begin{itemize}
  \item Full ENCODE accession table for all datasets (IP rep1, IP rep2, SMInput) — the 5 original + 20 new 2026-07 datasets.
  \item Complete per-dataset results table (composite, reproducibility, motif hit-rate + enrichment, recall, $n$ sites) for both optimizers.
  \item Per-dataset convergence plots (composite vs.\ iteration, LLM vs.\ Optuna) that do not fit in Results.
  \item Full PWM-database inventory used for motif scoring (mCrossBase, ATtRACT, RBPmap, oRNAment, RBPDB) and how the target motif is auto-resolved per RBP.
  \item \TODO{Decide what belongs here vs.\ in §7 Appendix, then remove this stub or merge the two appendix files.}
\end{itemize}
