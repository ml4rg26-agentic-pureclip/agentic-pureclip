\section{Appendix}
\label{appendix}

\scaffold{Bullets --- include if the page budget allows.}
\begin{itemize}
  \item Full 7-parameter table with bounds, types, and descriptions.
  \item \texttt{score\_report.json} field reference (every output field the scorer writes).
  \item Full LLM decision-prompt template (priors + delta table + bounds).
  \item Per-dataset convergence plots not shown in Results.
  \item Dashboard screenshots (per-dataset Runs view, binding-``docking'' landscape) as evidence of the reproducible tooling.
  \item \TODO{This section (\S7) and \texttt{8\_appendix.tex} (\S\ref{supplementary}) overlap --- decide on one appendix and remove the other; the second file was only added to fix a broken \texttt{\textbackslash include}.}
\end{itemize}

% Include here if page budget allows:
%   A. Full 7-parameter table with bounds, types, and descriptions.
%   B. score_report.json field reference (all output fields).
%   C. LLM prompt template (full text of the decision prompt).
%   D. Per-dataset convergence plots if not all fit in Results.
